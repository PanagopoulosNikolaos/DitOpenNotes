/*
 * Άσκηση 7: Έλεγχος Ροής (If/Else, Switch)
 *
 * Οδηγίες:
 * 1. Δημιουργήστε ένα πρόγραμμα που ζητά από τον χρήστη την ηλικία του και καθορίζει αν είναι παιδί (<13), έφηβος (13-19), ενήλικας (20-64) ή ηλικιωμένος (65+)
 * 2. Χρησιμοποιήστε κλίμακα if-else if για την υλοποίηση της λογικής
 * 3. Εκτυπώστε ένα κατάλληλο μήνυμα για κάθε κατηγορία
 * 4. Συμπεριλάβετε έλεγχο εγκυρότητας για να διασφαλίσετε ότι η ηλικία που εισάγεται είναι λογική (0-150)
 */

#include <iostream>
using namespace std;

int main() {
    // Ο κώδικάς σας εδώ
    
    return 0;
}