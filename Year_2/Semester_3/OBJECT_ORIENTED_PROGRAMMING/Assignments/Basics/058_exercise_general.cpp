/*
 * Άσκηση 13: Σύστημα Χαρακτήρα Παιχνιδιού
 *
 * Δημιουργήστε μια αφηρημένη βασική κλάση Character με:
 * - Προστατευόμενα μέλη: name (string), health (int), level (int)
 * - Κατασκευαστή που αρχικοποιεί όλα τα μέλη
 * - Καθαρά εικονικές μέθοδοι attack(), defend(), και specialAbility()
 * - Εικονική μέθοδο displayInfo()
 * - Εικονικό καταστροφέα
 *
 * Δημιουργήστε δύο παράγωγες κλάσεις:
 * - Warrior: επιπλέον μέλη strength (int), weaponType (string);
 *   υλοποιήστε όλες τις εικονικές μεθόδους με συμπεριφορά ειδική για πολεμιστή
 * - Mage: επιπλέον μέλη mana (int), spellType (string);
 *   υλοποιήστε όλες τις εικονικές μεθόδους με συμπεριφορά ειδική για μάγο
 *
 * Δημιουργήστε μια κλάση PlayerParty με:
 * - Ιδιωτικό μέλος: characters (διάνυσμα δεικτών Character)
 * - Μέθοδο addCharacter(Character*) για προσθήκη πολεμιστών ή μάγων
 * - Μέθοδο battle(Character* enemy) όπου όλα τα μέλη της ομάδας επιτίθενται στον εχθρό
 * - Μέθοδο healParty() που αποκαθιστά την υγεία όλων των μελών
 * - Μέθοδο displayParty() που εμφανίζει όλους τους χαρακτήρες
 * - Καταστροφέα που απελευθερώνει όλη τη μνήμη
 *
 * Στο main(), δημιουργήστε μια ομάδα με 2 πολεμιστές και 3 μάγους από είσοδο χρήστη,
 * εμφανίστε την ομάδα, προσομοιώστε μάχη με έναν εχθρό (δημιουργημένο παρόμοια),
 * δείξτε τη θεραπεία μετά τη μάχη, και βεβαιωθείτε ότι η καθαριότητα γίνεται σωστά.
 */

#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    // Ο κώδικάς σας εδώ

    return 0;
}
