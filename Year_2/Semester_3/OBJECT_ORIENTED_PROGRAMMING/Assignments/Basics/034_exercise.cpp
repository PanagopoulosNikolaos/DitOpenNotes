/*
 * Exercise 34: The `this` Pointer
 *
 * Instructions:
 * 1. Create a class called Calculator with methods for basic operations (add, subtract, multiply, divide)
 * 2. Use the `this` pointer to return the current object from each operation method
 * 3. Implement method chaining so that operations can be chained together (e.g., calc.add(5).subtract(3).multiply(2))
 * 4. Implement a method to compare the current calculator's value with another calculator object
 * 5. Use `this` to distinguish between member variables and parameters in setter methods
 */

#include <iostream>
using namespace std;

int main() {
    // Your code here
    
    return 0;
}
