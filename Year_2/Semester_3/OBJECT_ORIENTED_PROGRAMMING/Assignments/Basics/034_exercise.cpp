/*
 * Άσκηση 34: Ο Δείκτης `this`
 *
 * Οδηγίες:
 * 1. Δημιουργήστε μια κλάση που ονομάζεται Calculator με μεθόδους για βασικές πράξεις (πρόσθεση, αφαίρεση, πολλαπλασιασμός, διαίρεση)
 * 2. Χρησιμοποιήστε τον δείκτη `this` για να επιστρέφετε το τρέχον αντικείμενο από κάθε μέθοδο πράξης
 * 3. Υλοποιήστε αλυσιδωτή κλήση μεθόδων ώστε οι πράξεις να μπορούν να συνδυάζονται (π.χ., calc.add(5).subtract(3).multiply(2))
 * 4. Υλοποιήστε μια μέθοδο για σύγκριση της τρέχουσας τιμής του υπολογιστή με ένα άλλο αντικείμενο Calculator
 * 5. Χρησιμοποιήστε το `this` για να διακρίνετε μεταξύ μεταβλητών μελών και παραμέτρων στις μεθόδους ρύθμισης
 */

#include <iostream>
using namespace std;

int main() {
    // Ο κώδικάς σας εδώ
    
    return 0;
}